// app/(tabs)/add.tsx
// This file is used to redirect the user to the "Add" page when they click on the "Add" tab in the bottom tab bar. Routing is handled by StickyTabBar.
const AddTabRedirect = () => {
  return null;
};

export default AddTabRedirect;
